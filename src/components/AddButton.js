import * as React from 'react';
import Button from '@mui/material/Button';

export default function AdddButton() {
  return (
      <Button sx={{width: '30%', height: '50%'}} variant="contained">add</Button>
      
  );
}
